# W1504-06 — test (active-plan-id message box)

**Tier D lightweight.** Single-file plugin that message-boxes the active plan's `Id` — a one-line context
sanity check.

- **ESAPI surface:** `ScriptContext.PlanSetup.Id`.
- **Pointer:** hello-world stub; twin of [W1504-05 hello](W1504-05-hello.md) (the two W1504 `Plugins/`
  stubs).
